{
  "name": "lan-task-board",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight LAN task board for browser-based collaboration.",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "build:exe": "node scripts/build-exe.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/build-exe.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "postject": "^1.0.0-alpha.6"
  }
}
